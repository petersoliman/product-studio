{% extends 'base.html.twig' %}

{% block title %}Content Generator - Product Studio Intelligence{% endblock %}

{% block stylesheets %}
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        color: #333;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
    }

    /* Header */
    .header {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        padding: 1rem 0;
    }

    .header-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        font-size: 1.8rem;
        font-weight: 700;
        color: #667eea;
        text-decoration: none;
    }

    .nav-links {
        display: flex;
        gap: 2rem;
        list-style: none;
    }

    .nav-links a {
        color: #333;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
    }

    .nav-links a:hover {
        color: #667eea;
    }

    /* Hero Section */
    .hero {
        padding: 120px 0 80px;
        text-align: center;
        color: white;
    }

    .hero h1 {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 1rem;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
    }

    .hero h2 {
        font-size: 2.5rem;
        font-weight: 600;
        margin-bottom: 2rem;
        color: #f8f9fa;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
    }

    .hero p {
        font-size: 1.3rem;
        margin-bottom: 3rem;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
        line-height: 1.6;
        text-shadow: 0 1px 5px rgba(0, 0, 0, 0.3);
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn {
        padding: 1rem 2rem;
        border: none;
        border-radius: 50px;
        font-size: 1.1rem;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        cursor: pointer;
        display: inline-block;
    }

    .btn-primary {
        background: linear-gradient(45deg, #667eea, #764ba2);
        color: white;
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.2);
        color: white;
        border: 2px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(10px);
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.3);
        border-color: rgba(255, 255, 255, 0.5);
        transform: translateY(-2px);
    }

    /* Features Section */
    .features {
        padding: 80px 0;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
    }

    .features h3 {
        text-align: center;
        font-size: 2.5rem;
        margin-bottom: 3rem;
        color: #333;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
    }

    .feature-card {
        background: white;
        padding: 2rem;
        border-radius: 15px;
        box-shadow: 0 5px 25px rgba(0, 0, 0, 0.1);
        text-align: center;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 35px rgba(0, 0, 0, 0.15);
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
        color: #667eea;
    }

    .feature-card h4 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: #333;
    }

    .feature-card p {
        color: #666;
        line-height: 1.6;
    }

    /* API Section */
    .api-section {
        padding: 80px 0;
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    }

    .api-section h3 {
        text-align: center;
        font-size: 2.5rem;
        margin-bottom: 3rem;
        color: #333;
    }

    .api-endpoints {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-top: 3rem;
    }

    .endpoint-card {
        background: white;
        padding: 1.5rem;
        border-radius: 10px;
        border-left: 4px solid #667eea;
        box-shadow: 0 3px 15px rgba(0, 0, 0, 0.1);
    }

    .endpoint-method {
        display: inline-block;
        padding: 0.3rem 0.8rem;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    .method-post { background: #28a745; color: white; }
    .method-get { background: #007bff; color: white; }
    .method-delete { background: #dc3545; color: white; }

    .endpoint-path {
        font-family: 'Courier New', monospace;
        color: #333;
        font-weight: 600;
    }

    .endpoint-desc {
        color: #666;
        margin-top: 0.5rem;
        font-size: 0.9rem;
    }

    /* Footer */
    .footer {
        background: #333;
        color: white;
        text-align: center;
        padding: 2rem 0;
    }

    .footer p {
        margin-bottom: 0.5rem;
    }

    .footer a {
        color: #667eea;
        text-decoration: none;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .hero h1 {
            font-size: 2.5rem;
        }
        
        .hero h2 {
            font-size: 2rem;
        }
        
        .nav-links {
            display: none;
        }
        
        .cta-buttons {
            flex-direction: column;
            align-items: center;
        }
        
        .btn {
            width: 100%;
            max-width: 300px;
        }
    }
</style>
{% endblock %}

{% block body %}
    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="#" class="logo">🚀 Product Studio</a>
                <nav>
                    <ul class="nav-links">
                        <li><a href="#features">Features</a></li>
                        <li><a href="#api">API</a></li>
                        <li><a href="/api/health">Health</a></li>
                        <li><a href="/api/schema">Docs</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1>Content Generator</h1>
            <h2>AI-Powered Product Intelligence</h2>
            <p>Transform your product data into SEO-optimized, engaging content with our advanced AI technology. Generate compelling descriptions, discover relevant keywords, and enhance your product listings automatically.</p>
            <div class="cta-buttons">
                <a href="#api" class="btn btn-primary">Get Started</a>
                <a href="/api/schema" class="btn btn-secondary">View API Docs</a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="container">
            <h3>Powerful Features</h3>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">🤖</div>
                    <h4>AI Content Generation</h4>
                    <p>Automatically generate SEO-optimized product descriptions, titles, and meta content using advanced AI algorithms.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔍</div>
                    <h4>Keyword Research</h4>
                    <p>Discover high-performing keywords and search terms to improve your product visibility and search rankings.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h4>Data Enrichment</h4>
                    <p>Enhance your product data with additional information, specifications, and structured data for better search results.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🚀</div>
                    <h4>Fast Processing</h4>
                    <p>Lightning-fast API responses with intelligent caching and optimization for high-volume product processing.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔄</div>
                    <h4>Real-time Updates</h4>
                    <p>Get real-time processing status and updates as your content is being generated and optimized.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h4>Mobile Optimized</h4>
                    <p>Responsive design and mobile-first approach for seamless experience across all devices.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- API Section -->
    <section class="api-section" id="api">
        <div class="container">
            <h3>API Endpoints</h3>
            <div class="api-endpoints">
                <div class="endpoint-card">
                    <span class="endpoint-method method-post">POST</span>
                    <div class="endpoint-path">/api/products/intelligence</div>
                    <div class="endpoint-desc">Process product intelligence and generate optimized content</div>
                </div>
                <div class="endpoint-card">
                    <span class="endpoint-method method-get">GET</span>
                    <div class="endpoint-path">/api/products</div>
                    <div class="endpoint-desc">List all products with filtering and pagination</div>
                </div>
                <div class="endpoint-card">
                    <span class="endpoint-method method-get">GET</span>
                    <div class="endpoint-path">/api/products/{id}</div>
                    <div class="endpoint-desc">Get complete product details and SEO data</div>
                </div>
                <div class="endpoint-card">
                    <span class="endpoint-method method-get">GET</span>
                    <div class="endpoint-path">/api/products/{id}/seo</div>
                    <div class="endpoint-desc">Get SEO-optimized product data</div>
                </div>
                <div class="endpoint-card">
                    <span class="endpoint-method method-get">GET</span>
                    <div class="endpoint-path">/api/health</div>
                    <div class="endpoint-desc">Check API health and status</div>
                </div>
                <div class="endpoint-card">
                    <span class="endpoint-method method-get">GET</span>
                    <div class="endpoint-path">/api/schema</div>
                    <div class="endpoint-desc">View complete API documentation</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Product Studio Intelligence. All rights reserved.</p>
            <p>Powered by <a href="https://symfony.com">Symfony</a> and AI Technology</p>
        </div>
    </footer>
{% endblock %}
